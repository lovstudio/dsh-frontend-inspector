# Registers the Lovstudio frontend-inspector Host plugin. It builds the clean
# DSH checkout into an external Lovinsp-instrumented cache, then uses the Host
# webServer extension points to serve that shell without modifying the checkout.

- insert:
    - id: frontend-inspector
      name: '@lovstudio/dsh-frontend-inspector'
      config:
        enabled: true
